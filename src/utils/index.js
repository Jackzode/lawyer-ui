

import {request} from './request'

import {
    localStorageSetToken,
    localStorageGetToken,
    localStorageRemoveToken
} from './token'

export {
    request,
    localStorageSetToken,
    localStorageGetToken,
    localStorageRemoveToken
}